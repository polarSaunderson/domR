##
test_function <- function(name = "Dominic") {
  #' Test that the package has loaded correctly and the functions are accessible
  #'
  #' @param name "string": user name
  #'
  #' @export

  # Code ----------------------------------------------------------------------!
  cat(paste0("\nHi ", name, ", let's go solve Antarctica!"))
  print_line()
}
